// Program to add key/value pair to an object

function addKeyValuePairToObject(key,value){
    let newObj = {}
    newObj[key] = value
    return newObj
}
console.log(addKeyValuePairToObject('name','Manish'))